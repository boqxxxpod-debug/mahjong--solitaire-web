import { defineConfig } from 'vite';

export default defineConfig({
  // Relative asset URLs work both at a custom domain root and under a
  // GitHub Pages project path such as /mahjong--solitaire-web/.
  base: './',
});
